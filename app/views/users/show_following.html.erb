<!-- <h1> -->
<!--   <%= gravatar_for @user %> -->
<!--   <%= @user.name %> -->
<!-- </h1> -->
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <!-- <% @users.each do |user| %> -->
  <!-- <\!-- <%= user.followed.name %><br> -\-> -->
  <!-- <\!-- <%= link_to(user.followed.id) %> -\-> -->
  <!-- <%= user.followed.name %><br> -->
  <!-- <% end %> -->
  <div class="col-md-8">
    <h3>Following Users</h3>
    <% @users.each do |user| %>
    <ol class="following">
      <!-- <%= user.followed.name %><br> -->
      <!-- <%= link_to(user.followed.id) %> -->
      <!-- <%= user.followed.name %> -->
      <%= gravatar_for(user.followed, size: 20) %>
      <%= link_to user.followed.name, user_path(user.followed.id)%>
      <!-- <%= link_to(user.followed.name) %> -->
      </ol>
    <% end %>
  </div>
</div>
